# Backend API base URL (must end with /api)
# Development:
# VITE_API_URL=http://localhost:5000/api
# Production (set when building):
# VITE_API_URL=https://your-api.com/api

# Optional: Socket.IO server URL (defaults to same host as API without /api)
# VITE_SOCKET_URL=https://your-api.com
